<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:targetSandboxVersion="1"
    package="common.wallet.appone">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppOne">
        <activity
            android:name=".InviteCode"
            android:parentActivityName=".Wallets">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Wallets" />
        </activity>
        <activity
            android:name=".EditRecord"
            android:parentActivityName=".WalletRecords">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".WalletRecords" />
        </activity>
        <activity
            android:name=".WalletRecords"
            android:parentActivityName=".Wallets">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Wallets" />
        </activity>
        <activity
            android:name=".CreateRecord"
            android:parentActivityName=".WalletRecords">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".WalletRecords" />
        </activity>
        <activity
            android:name=".CreateWallet"
            android:parentActivityName=".Wallets">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Wallets" />
        </activity>
        <activity android:name=".Wallets">

            <!-- android:parentActivityName=".MainActivity"> -->
            <!-- <meta-data -->
            <!-- android:name="android.support.PARENT_ACTIVITY" -->
            <!-- android:value=".MainActivity" /> -->
            <!-- </activity> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>